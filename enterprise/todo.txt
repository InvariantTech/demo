vlan10 should be employees
* access to internet
* access to all web servers in data center
* access to all web servers in dmz
* no access to finance servers in dc

vlan20 should be finance(sensitive)
* all vlan10 access
* access to finance servers in DC

vlan30 should be printers/building stuff
* access specific points on internet
* no access to vlan10/20/40
* access to single server in DC

vlan40 should be priviledged
* access to internet
* access to all web servers in data center
* access to all web servers in dmz
* ssh to all servers


internet
only access to the dmz on web